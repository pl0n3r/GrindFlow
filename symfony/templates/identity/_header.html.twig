<header class="navbar identity-navbar">
    <a class="brand" href="{{ path('grindflow_home') }}" aria-label="GrindFlow, inicio">GRIND<span>FLOW</span><span class="brand-dot" aria-hidden="true">●</span></a>
    <nav aria-label="Navegación de acceso">
        <a href="{{ path('grindflow_home') }}">Inicio</a>
        <a class="identity-preview-link" href="{{ path('grindflow_preview') }}">Vista previa</a>
        <span class="identity-current" aria-current="page">{{ current_label }}</span>
    </nav>
</header>
